<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data noupdate="0">
	
	<!-- Show only own customers -->
	<record id="group_own_customer" model="res.groups">
      <field name="name">Show Own Customer</field>
      <field name="implied_ids" eval="[]"/>
      <!-- <field name="category_id" ref=""/> -->
    </record>
    
    <!-- <record id="group_own_customer_rule" model="ir.rule">
        <field name="name">Show Own Customers</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="domain_force">[('create_uid','=',user.id)]</field>
        <field name="groups" eval="[(4, ref('group_own_customer'))]"/>
    </record> -->
    
</data>
</odoo>
